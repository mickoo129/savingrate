# Netlify 部署設定

[build]
  # 靜態檔案直接從根目錄發佈
  publish = "."
  # Netlify Functions 目錄
  functions = "netlify/functions"

[functions]
  # 使用 esbuild 打包 Functions (輕量快速)
  node_bundler = "esbuild"

# 開發伺服器
[dev]
  port = 8888
  publish = "."
